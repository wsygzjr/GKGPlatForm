<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 xmlns:ui="using:GKG.UI"
			 xmlns:views="clr-namespace:GKG.UI.General"
			 xmlns:local="using:GKG.UI.General"
			 xmlns:vision="using:AvaloniaVisionControl"
			 xmlns:vm="clr-namespace:GKG.UI.General"
			 x:DataType="vm:IOStateInitViewModel"
			 xmlns:custom="clr-namespace:GKG.UI;assembly=GKG.UI"
			 x:Class="GKG.UI.General.IOStateInitView">

	<!-- IO 状态初始化参数配置界面View -->
	<Grid Margin="8" HorizontalAlignment="Stretch" VerticalAlignment="Top" RowDefinitions="auto,auto,auto">
		<!-- 标题 -->
		<Label Grid.Row="0" Content="IO状态初始化参数配置界面" FontSize="20" HorizontalAlignment="Left" Margin="0 0 0 10" />

		<!-- 下拉框：IO 设备 ID -->
		<custom:ComboxControl
		Grid.Row="1"
		HorizontalAlignment="Center"
		HorizontalContentAlignment="Center"
		VerticalContentAlignment="Center"
		LabelName="IO设备ID:"
		WidthLevel="Medium3"
		ViewModel="{Binding IODeviceIDViewModel}" />

		<!-- 下拉框：IO 通道号 -->
		<custom:ComboxControl
		Grid.Row="2"
		HorizontalAlignment="Center"
		HorizontalContentAlignment="Center"
		VerticalContentAlignment="Center"
		LabelName="IO通道号:"
		WidthLevel="Medium3"
		ViewModel="{Binding IOChannelViewModel}" />
	</Grid>

</UserControl>