using Avalonia.Controls;
using Avalonia.Markup.Xaml;

namespace Griffins.CompUI.AxisDebug.CompUI_AxisDebug.PageType.InitCfgPage.AxisDebugging.Views
{
    public partial class AxisDebuggingCompUIView : UserControl
    {
        public AxisDebuggingCompUIView()
        {
            InitializeComponent();
        }

        private void InitializeComponent()
        {
            AvaloniaXamlLoader.Load(this);
        }
    }
}
