using Avalonia.Controls;
using Avalonia.Markup.Xaml;

namespace DispensingPageType.Views.DebuggingPage.AxisDebugging
{
    public partial class AxisDebuggingSubPageView : UserControl
    {
        public AxisDebuggingSubPageView()
        {
            InitializeComponent();
        }

        private void InitializeComponent()
        {
            AvaloniaXamlLoader.Load(this);
        }
    }
}
